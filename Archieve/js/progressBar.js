var addProgressBar = ( function ( ) {

  var objMap {
    // описываем экземпляр объекта
    // включая методы
  }
  
  objMap.prototype = {
    // описываем общие методы
    // и свойства
  }

  return construct( ) {
    // действия при инициализации
  
    return objMap
  }
  
} ) ( )