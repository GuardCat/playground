var mediator = ( function( ) {

  function addMediatorListener(ev, fn) {
    this
  }
  
  return {
    addMediatorListener: addMediatorListener,
    generateEvent: generateEvent,
    eventsPool: {},
    
    
  }

} )( );